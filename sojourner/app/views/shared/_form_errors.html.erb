<% if record.errors.any? %>
  <div class="alert alert-danger">
    <h4><%= pluralize(record.errors.count, "error") %> prohibited this <%= record.class.name.underscore.humanize %> from being saved:</h4>

    <ul>
      <% record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
